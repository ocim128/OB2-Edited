<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:OpenBullet2.Native.Converters"
                    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">

    <!-- Converters -->
    <converters:LessThanConverter x:Key="LessThanConverter" />
    <BooleanToVisibilityConverter x:Key="BoolToVis" />



    <!-- Square CheckBox Style -->
    <Style x:Key="SquareCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="#E2E8F0" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="CheckBoxBorder"
                                Width="16"
                                Height="16"
                                Background="#2D3748"
                                BorderBrush="#4A5568"
                                BorderThickness="1"
                                CornerRadius="2"
                                Margin="0,0,8,0">
                            <Path x:Name="CheckMark"
                                  Width="10"
                                  Height="8"
                                  Stretch="Fill"
                                  Fill="#10B981"
                                  Data="M 0,4 L 3,7 L 8,0"
                                  Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            <Setter TargetName="CheckBoxBorder" Property="Background" Value="#064E3B" />
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#10B981" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Simplified Start Button Style -->
    <Style x:Key="PerfectStartButton" TargetType="Button">
        <Setter Property="Background" Value="#10B981" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Content" Value="▶ Start" />
    </Style>

    <!-- Simplified Stop Button Style -->
    <Style x:Key="PerfectStopButton" TargetType="Button">
        <Setter Property="Background" Value="#EF4444" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Content" Value="⏹ Stop" />
    </Style>



</ResourceDictionary>