<UserControl x:Class="Flux.Native.Views.Controls.Config.ConfigStackerInspectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="400">
    <Border Background="{DynamicResource Modern.BackgroundSecondary}"
            BorderBrush="{DynamicResource Modern.BorderSecondary}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="16">
        <Grid>
            <TextBlock x:Name="BlockInfoPlaceholder"
                       Text="Select a block to inspect its settings."
                       Foreground="{StaticResource Modern.ForegroundMuted}"
                       FontSize="13"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Margin="32" />
            <ScrollViewer x:Name="BlockInfoScrollViewer"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ContentControl x:Name="BlockInfoContent" />
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
