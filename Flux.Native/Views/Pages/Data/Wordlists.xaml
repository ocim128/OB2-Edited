<Page x:Class="Flux.Native.Views.Pages.Data.Wordlists"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Flux.Native.Views.Pages"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="780"
      Title="Wordlists"
      TextOptions.TextFormattingMode="Display"
      TextOptions.TextRenderingMode="ClearType"
      RenderOptions.ClearTypeHint="Enabled"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True">

    <Grid Margin="24,20,24,24" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ModernCardLite}" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
            <Button x:Name="addButton" Click="Add" Margin="0,0,8,0" Style="{StaticResource StyledSuccessButton}" >
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterialDesign Kind="Add" Width="16" Height="16" />
                    <TextBlock VerticalAlignment="Center" Margin="8,0,0,0" FontWeight="SemiBold">Add</TextBlock>
                </StackPanel>
            </Button>
            <Button x:Name="deleteButton" Click="DeleteSelected" Margin="0,0,8,0" Style="{StaticResource StyledDangerButton}" >
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Close" Width="16" Height="16" />
                    <TextBlock VerticalAlignment="Center" Margin="8,0,0,0" FontWeight="SemiBold">Delete</TextBlock>
                </StackPanel>
            </Button>
            <Button x:Name="deleteAllButton" Click="DeleteAll" Margin="0,0,8,0" Style="{StaticResource StyledDangerButton}" >
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Close" Width="16" Height="16" />
                    <TextBlock VerticalAlignment="Center" Margin="8,0,0,0" FontWeight="SemiBold">Delete All</TextBlock>
                </StackPanel>
            </Button>
            <Button x:Name="deleteNotFoundWordlistsButton" Click="DeleteNotFound" Margin="0,0,8,0" Style="{StaticResource StyledDangerButton}" ToolTip="Deletes missing wordlists from the list.">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Close" />
                    <TextBlock VerticalAlignment="Center" Margin="5 0">Delete Not Found</TextBlock>
                </StackPanel>
            </Button>
            <Button x:Name="exportSelectedButton" Click="ExportSelected" Margin="0,0,16,0" Style="{StaticResource StyledPrimaryButton}">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Export" />
                    <TextBlock VerticalAlignment="Center" Margin="5 0">Export Selected</TextBlock>
                </StackPanel>
            </Button>
            <TextBox x:Name="filterTextbox" Text="{Binding SearchString, UpdateSourceTrigger=PropertyChanged, Delay=200}" Margin="0,0,8,0" Width="150" Style="{StaticResource MatchingTextBox}"/>
        </StackPanel>
        </Border>

        <ListView x:Name="wordlistListView" Grid.Row="1" Foreground="{DynamicResource ForegroundMain}" Background="Transparent" ItemsSource="{Binding Path=WordlistsCollection}" SelectionMode="Extended" BorderThickness="0" AllowDrop="True" Drop="HandleDrop" Style="{StaticResource FluxVirtualizedListViewStyle}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" VirtualizingPanel.ScrollUnit="Item" VirtualizingPanel.CacheLengthUnit="Item" VirtualizingPanel.CacheLength="30,30" UseLayoutRounding="True" SnapsToDevicePixels="True" ScrollViewer.CanContentScroll="True" ScrollViewer.IsDeferredScrollingEnabled="True" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.Resources>
                <Style TargetType="{x:Type GridViewColumnHeader}" BasedOn="{StaticResource FluxGridViewColumnHeaderStyle}" />
                <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource FluxListViewItemStyle}" />
            </ListView.Resources>

            <ListView.View>
                <GridView>
                    <GridViewColumn Width="30">
                        <GridViewColumn.Header>
                            <CheckBox x:Name="selectAllCheckbox" ToolTip="Select All" Checked="SelectAll" Unchecked="DeselectAll" />
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Name}">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Name" Click="ColumnHeaderClicked">Name</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn Width="250" DisplayMemberBinding="{Binding Path=FileName}">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="FileName" Click="ColumnHeaderClicked">Path</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Type}">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Type" Click="ColumnHeaderClicked">Type</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Purpose}">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Purpose" Click="ColumnHeaderClicked">Purpose</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Total}">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Total" Click="ColumnHeaderClicked">Total</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
