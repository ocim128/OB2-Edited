<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:Flux.Native.Converters"
                    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">

        <!-- Converters -->
    <converters:LessThanConverter x:Key="LessThanConverter" />
    <BooleanToVisibilityConverter x:Key="BoolToVis" />
    <converters:BoolToColorConverter x:Key="BoolToColorConverter" />
    <converters:BoolToIconConverter x:Key="BoolToIconConverter" />


    <!-- Square CheckBox Style -->
    <Style x:Key="SquareCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource Modern.ForegroundMain}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="CheckBoxBorder"
                                Width="16"
                                Height="16"
                                Background="{DynamicResource Modern.BackgroundInput}"
                                BorderBrush="{DynamicResource Modern.BorderInput}"
                                BorderThickness="1"
                                CornerRadius="2"
                                Margin="0,0,8,0">
                            <Path x:Name="CheckMark"
                                  Width="10"
                                  Height="8"
                                  Stretch="Fill"
                                  Fill="{DynamicResource Modern.Success}"
                                  Data="M 0,4 L 3,7 L 8,0"
                                  Visibility="Collapsed" />
                        </Border>
                        <AccessText Text="{TemplateBinding Content}"
                                    VerticalAlignment="Center"
                                    Foreground="{TemplateBinding Foreground}"
                                    FontSize="{TemplateBinding FontSize}"
                                    FontWeight="{TemplateBinding FontWeight}" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource Modern.BackgroundHover}" />
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource Modern.Success}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Shared debugger surfaces and controls -->
    <Style x:Key="DebuggerSectionBorder" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Modern.BackgroundSecondary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Modern.BorderSecondary}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="DebuggerSectionTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Modern.ForegroundSecondary}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="DebuggerInlineLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Modern.ForegroundSecondary}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="11.5" />
        <Setter Property="FontWeight" Value="Medium" />
    </Style>

    <Style x:Key="DebuggerCompactOptionGroup" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Modern.BackgroundMain}" />
        <Setter Property="BorderBrush" Value="{StaticResource Modern.BorderSecondary}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="Margin" Value="0,0,6,6" />
    </Style>

    <Style x:Key="DebuggerToolbarButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
        <Setter Property="MinHeight" Value="28" />
        <Setter Property="MinWidth" Value="28" />
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Padding" Value="6" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="{StaticResource Modern.BackgroundInput}" />
        <Setter Property="BorderBrush" Value="{StaticResource Modern.BorderSecondary}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Modern.BackgroundHover}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource Modern.BackgroundTertiary}" />
                <Setter Property="BorderBrush" Value="{StaticResource Modern.BorderFocus}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.45" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DebuggerCommandButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
        <Setter Property="Padding" Value="14,6" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Background" Value="{StaticResource Modern.BackgroundInput}" />
        <Setter Property="BorderBrush" Value="{StaticResource Modern.BorderSecondary}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Modern.BackgroundHover}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource Modern.BackgroundTertiary}" />
                <Setter Property="BorderBrush" Value="{StaticResource Modern.BorderFocus}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DebuggerTabButton" TargetType="Button">
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Margin" Value="0,0,2,0" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.9" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.8" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Use global consolidated action buttons from App.xaml -->
    <Style x:Key="PerfectStartButton" TargetType="Button" BasedOn="{StaticResource BaseActionButton}">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#10B981" Offset="0"/>
                    <GradientStop Color="#059669" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Content" Value="▶ Start" />
    </Style>

    <Style x:Key="PerfectStopButton" TargetType="Button" BasedOn="{StaticResource BaseActionButton}">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#EF4444" Offset="0"/>
                    <GradientStop Color="#DC2626" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Content" Value="⏹ Stop" />
    </Style>



</ResourceDictionary>
